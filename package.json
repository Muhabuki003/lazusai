{
  "name": "lazusai",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant iMessage AI chatbot platform — Cloudflare Worker webhook receiver and admin router.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-worker.mjs",
    "dev": "wrangler pages dev dist --kv CLIENTS_KV",
    "deploy": "node scripts/build-worker.mjs && wrangler pages deploy",
    "sync-clients": "bash scripts/sync_clients_to_kv.sh"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
